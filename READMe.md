#TRABALHO PARA MOSTRAR O USO DO *GIT*

esse trabalho foi feito para ser mostrado as praticas básicas do git. Como o: git config --global user.name, git config --global user.email, git add . , git commit -m. E a publicação atraves do VsCode diretamente para o gitHub! 